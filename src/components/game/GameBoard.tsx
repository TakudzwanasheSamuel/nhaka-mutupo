"use client";

/**
 * Legacy GameBoard component — game logic has been moved to the main page.
 * Kept as a minimal export for backward compatibility.
 */
export function GameBoard() {
  return null;
}
